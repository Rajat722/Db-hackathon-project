import { DataAlertPipe } from './data-alert.pipe';

describe('DataAlertPipe', () => {
  it('create an instance', () => {
    const pipe = new DataAlertPipe();
    expect(pipe).toBeTruthy();
  });
});
